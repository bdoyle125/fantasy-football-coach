# Base URL of the backend API. Leave unset in local dev -- the Vite dev server proxies
# /api/* to http://localhost:5000 (see vite.config.ts). Set explicitly for a deployed
# build (e.g. https://your-backend.onrender.com/).
VITE_API_URL=
